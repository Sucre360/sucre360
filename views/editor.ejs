<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Modo Editor - Sucre 360</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header class="hero">
    <h1>Editor Sucre 360</h1>
  </header>

  <section class="editor">
    <% if (!loggedIn) { %>
      <% if (error) { %>
        <p style="color: red;"><%= error %></p>
      <% } %>
      <form action="/login" method="POST">
        <label>Usuario: <input type="text" name="username" required></label><br>
        <label>Contraseña: <input type="password" name="password" required></label><br>
        <button type="submit">Ingresar</button>
      </form>
    <% } else { %>
      <form action="/guardar" method="POST">
        <h2>Editar Contenido</h2>
        <label>Título Hero:
          <input type="text" name="heroTitulo" value="<%= contenido.heroTitulo %>">
        </label><br>
        <label>Descripción Hero:<br>
          <textarea name="heroDescripcion" rows="4"><%= contenido.heroDescripcion %></textarea>
        </label><br>
        <label>¿Quiénes Somos?:<br>
          <textarea name="quienesSomos" rows="6"><%= contenido.quienesSomos %></textarea>
        </label><br>

        <h3>Lugares Destacados</h3>
        <textarea name="lugares" style="width: 100%; height: 300px;"><%= JSON.stringify(contenido.lugares, null, 2) %></textarea><br>
        <button type="submit">Guardar Cambios</button>
      </form>
    <% } %>
  </section>

  <footer>
    <p>© Sucre 360</p>
  </footer>
</body>
</html>
