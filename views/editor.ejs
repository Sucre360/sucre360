<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Modo Editor - Sucre 360</title>
  <link rel="stylesheet" href="/css/styles.css">
</head>
<body>
  <header class="hero">
    <h1>Editor Sucre 360</h1>
  </header>

  <section class="editor">
    <% if (error) { %>
      <p style="color: red;"><%= error %></p>
    <% } %>
    <form action="/login" method="POST">
      <label>Usuario: <input type="text" name="username" required></label>
      <label>Contraseña: <input type="password" name="password" required></label>
      <button type="submit">Ingresar</button>
    </form>

    <hr>

    <form action="/guardar" method="POST">
      <h2>Editar Contenido</h2>
      <label>Título Hero:
        <input type="text" name="heroTitulo" value="<%= contenido.heroTitulo %>">
      </label>
      <label>Descripción Hero:
        <textarea name="heroDescripcion"><%= contenido.heroDescripcion %></textarea>
      </label>
      <label>¿Quiénes Somos?:
        <textarea name="quienesSomos"><%= contenido.quienesSomos %></textarea>
      </label>

      <h3>Editar Lugares Destacados</h3>
      <textarea name="lugares" style="width: 100%; height: 300px;">
<%= JSON.stringify(contenido.lugares, null, 2) %>
      </textarea>
      <button type="submit">Guardar Cambios</button>
    </form>
  </section>

  <footer>
    <p>© Sucre 360</p>
  </footer>
</body>
</html>
